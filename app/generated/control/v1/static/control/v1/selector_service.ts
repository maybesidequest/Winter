// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v7.35.1
// source: control/v1/selector_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { RequestContext, SelectorOption, SelectorType, selectorTypeFromJSON, selectorTypeToNumber } from "./models";

export const protobufPackage = "interchat.control.v1";

export interface SelectorReference {
  type: SelectorType;
  id: string;
  parentId: string;
}

export interface SearchSelectorsRequest {
  context?: RequestContext | undefined;
  type: SelectorType;
  query: string;
  parentId: string;
  limit: number;
  cursor: string;
}

export interface SearchSelectorsResponse {
  options: SelectorOption[];
  nextCursor: string;
  hasMore: boolean;
}

export interface ResolveSelectorsRequest {
  context?: RequestContext | undefined;
  references: SelectorReference[];
}

export interface ResolvedSelector {
  reference?: SelectorReference | undefined;
  option?: SelectorOption | undefined;
  valid: boolean;
  rejectionReason: string;
}

export interface ResolveSelectorsResponse {
  selectors: ResolvedSelector[];
}

function createBaseSelectorReference(): SelectorReference {
  return { type: SelectorType.SELECTOR_TYPE_UNSPECIFIED, id: "", parentId: "" };
}

export const SelectorReference: MessageFns<SelectorReference> = {
  encode(message: SelectorReference, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== SelectorType.SELECTOR_TYPE_UNSPECIFIED) {
      writer.uint32(8).int32(selectorTypeToNumber(message.type));
    }
    if (message.id !== "") {
      writer.uint32(18).string(message.id);
    }
    if (message.parentId !== "") {
      writer.uint32(26).string(message.parentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SelectorReference {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSelectorReference();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = selectorTypeFromJSON(reader.int32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.parentId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<SelectorReference>, I>>(base?: I): SelectorReference {
    return SelectorReference.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SelectorReference>, I>>(object: I): SelectorReference {
    const message = createBaseSelectorReference();
    message.type = object.type ?? SelectorType.SELECTOR_TYPE_UNSPECIFIED;
    message.id = object.id ?? "";
    message.parentId = object.parentId ?? "";
    return message;
  },
};

function createBaseSearchSelectorsRequest(): SearchSelectorsRequest {
  return {
    context: undefined,
    type: SelectorType.SELECTOR_TYPE_UNSPECIFIED,
    query: "",
    parentId: "",
    limit: 0,
    cursor: "",
  };
}

export const SearchSelectorsRequest: MessageFns<SearchSelectorsRequest> = {
  encode(message: SearchSelectorsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.type !== SelectorType.SELECTOR_TYPE_UNSPECIFIED) {
      writer.uint32(16).int32(selectorTypeToNumber(message.type));
    }
    if (message.query !== "") {
      writer.uint32(26).string(message.query);
    }
    if (message.parentId !== "") {
      writer.uint32(34).string(message.parentId);
    }
    if (message.limit !== 0) {
      writer.uint32(40).uint32(message.limit);
    }
    if (message.cursor !== "") {
      writer.uint32(50).string(message.cursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchSelectorsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchSelectorsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.type = selectorTypeFromJSON(reader.int32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.parentId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.cursor = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<SearchSelectorsRequest>, I>>(base?: I): SearchSelectorsRequest {
    return SearchSelectorsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchSelectorsRequest>, I>>(object: I): SearchSelectorsRequest {
    const message = createBaseSearchSelectorsRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.type = object.type ?? SelectorType.SELECTOR_TYPE_UNSPECIFIED;
    message.query = object.query ?? "";
    message.parentId = object.parentId ?? "";
    message.limit = object.limit ?? 0;
    message.cursor = object.cursor ?? "";
    return message;
  },
};

function createBaseSearchSelectorsResponse(): SearchSelectorsResponse {
  return { options: [], nextCursor: "", hasMore: false };
}

export const SearchSelectorsResponse: MessageFns<SearchSelectorsResponse> = {
  encode(message: SearchSelectorsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.options) {
      SelectorOption.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.nextCursor !== "") {
      writer.uint32(18).string(message.nextCursor);
    }
    if (message.hasMore !== false) {
      writer.uint32(24).bool(message.hasMore);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchSelectorsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchSelectorsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.options.push(SelectorOption.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nextCursor = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.hasMore = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<SearchSelectorsResponse>, I>>(base?: I): SearchSelectorsResponse {
    return SearchSelectorsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchSelectorsResponse>, I>>(object: I): SearchSelectorsResponse {
    const message = createBaseSearchSelectorsResponse();
    message.options = object.options?.map((e) => SelectorOption.fromPartial(e)) || [];
    message.nextCursor = object.nextCursor ?? "";
    message.hasMore = object.hasMore ?? false;
    return message;
  },
};

function createBaseResolveSelectorsRequest(): ResolveSelectorsRequest {
  return { context: undefined, references: [] };
}

export const ResolveSelectorsRequest: MessageFns<ResolveSelectorsRequest> = {
  encode(message: ResolveSelectorsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(10).fork()).join();
    }
    for (const v of message.references) {
      SelectorReference.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolveSelectorsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveSelectorsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.references.push(SelectorReference.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResolveSelectorsRequest>, I>>(base?: I): ResolveSelectorsRequest {
    return ResolveSelectorsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolveSelectorsRequest>, I>>(object: I): ResolveSelectorsRequest {
    const message = createBaseResolveSelectorsRequest();
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.references = object.references?.map((e) => SelectorReference.fromPartial(e)) || [];
    return message;
  },
};

function createBaseResolvedSelector(): ResolvedSelector {
  return { reference: undefined, option: undefined, valid: false, rejectionReason: "" };
}

export const ResolvedSelector: MessageFns<ResolvedSelector> = {
  encode(message: ResolvedSelector, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reference !== undefined) {
      SelectorReference.encode(message.reference, writer.uint32(10).fork()).join();
    }
    if (message.option !== undefined) {
      SelectorOption.encode(message.option, writer.uint32(18).fork()).join();
    }
    if (message.valid !== false) {
      writer.uint32(24).bool(message.valid);
    }
    if (message.rejectionReason !== "") {
      writer.uint32(34).string(message.rejectionReason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolvedSelector {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolvedSelector();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reference = SelectorReference.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.option = SelectorOption.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.valid = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.rejectionReason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResolvedSelector>, I>>(base?: I): ResolvedSelector {
    return ResolvedSelector.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolvedSelector>, I>>(object: I): ResolvedSelector {
    const message = createBaseResolvedSelector();
    message.reference = (object.reference !== undefined && object.reference !== null)
      ? SelectorReference.fromPartial(object.reference)
      : undefined;
    message.option = (object.option !== undefined && object.option !== null)
      ? SelectorOption.fromPartial(object.option)
      : undefined;
    message.valid = object.valid ?? false;
    message.rejectionReason = object.rejectionReason ?? "";
    return message;
  },
};

function createBaseResolveSelectorsResponse(): ResolveSelectorsResponse {
  return { selectors: [] };
}

export const ResolveSelectorsResponse: MessageFns<ResolveSelectorsResponse> = {
  encode(message: ResolveSelectorsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.selectors) {
      ResolvedSelector.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResolveSelectorsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolveSelectorsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.selectors.push(ResolvedSelector.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResolveSelectorsResponse>, I>>(base?: I): ResolveSelectorsResponse {
    return ResolveSelectorsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResolveSelectorsResponse>, I>>(object: I): ResolveSelectorsResponse {
    const message = createBaseResolveSelectorsResponse();
    message.selectors = object.selectors?.map((e) => ResolvedSelector.fromPartial(e)) || [];
    return message;
  },
};

export type SelectorServiceDefinition = typeof SelectorServiceDefinition;
export const SelectorServiceDefinition = {
  name: "SelectorService",
  fullName: "interchat.control.v1.SelectorService",
  methods: {
    searchSelectors: {
      name: "SearchSelectors",
      requestType: SearchSelectorsRequest,
      requestStream: false,
      responseType: SearchSelectorsResponse,
      responseStream: false,
      options: {},
    },
    resolveSelectors: {
      name: "ResolveSelectors",
      requestType: ResolveSelectorsRequest,
      requestStream: false,
      responseType: ResolveSelectorsResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface SelectorServiceImplementation<CallContextExt = {}> {
  searchSelectors(
    request: SearchSelectorsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<SearchSelectorsResponse>>;
  resolveSelectors(
    request: ResolveSelectorsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ResolveSelectorsResponse>>;
}

export interface SelectorServiceClient<CallOptionsExt = {}> {
  searchSelectors(
    request: DeepPartial<SearchSelectorsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<SearchSelectorsResponse>;
  resolveSelectors(
    request: DeepPartial<ResolveSelectorsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ResolveSelectorsResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
